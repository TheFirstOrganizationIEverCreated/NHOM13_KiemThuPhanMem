<Window
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    Height="300"
    Title="Execise1"
    Width="500"
    mc:Ignorable="d"
    x:Class="SoftwareTestingExercisesOfChapter4And5.Exercise1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SoftwareTestingExercisesOfChapter4And5"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            HorizontalAlignment="Center"
            Orientation="Horizontal"
            VerticalAlignment="Center">
            <Label Content="x = " Width="30" />
            <TextBox
                Height="20"
                Text="{Binding ArgumentString, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
                TextChanged="ArgumentTextBoxChangedText"
                Width="100" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Center"
            Orientation="Horizontal"
            VerticalAlignment="Center">
            <Label Content="f(x) = " Width="40" />
            <TextBlock
                Height="20"
                Text="{Binding FunctionValueString, Mode=OneWay}"
                Width="100" />
        </StackPanel>
    </Grid>
</Window>