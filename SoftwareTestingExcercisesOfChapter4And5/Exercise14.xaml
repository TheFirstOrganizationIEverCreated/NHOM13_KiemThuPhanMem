<Window
    Height="300"
    ResizeMode="NoResize"
    Title="Exercise14"
    Width="600"
    WindowStartupLocation="CenterScreen"
    x:Class="SoftwareTestingExercisesOfChapter4And5.Exercise14"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,8"
            Orientation="Horizontal">
            <TextBlock
                Margin="0,0,8,0"
                Text="Enter n (5 ≤ n ≤ 100):"
                VerticalAlignment="Center" />
            <TextBox Name="InputN" Width="60" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Margin="0,0,0,8"
            Orientation="Horizontal">
            <TextBlock
                Margin="0,0,8,0"
                Text="Enter numbers (space-separated):"
                VerticalAlignment="Center" />
            <TextBox Name="InputNumbers" Width="250" />
            <Button
                Click="Process_Click"
                Content="Process"
                Margin="8,0,0,0" />
        </StackPanel>

        <TextBlock
            FontSize="14"
            Grid.Row="2"
            Name="ResultText"
            TextWrapping="Wrap" />
    </Grid>
</Window>
