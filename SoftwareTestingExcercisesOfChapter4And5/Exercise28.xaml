<Window
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    Height="350"
    Title="Exercise28"
    Width="700"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d"
    x:Class="SoftwareTestingExercisesOfChapter4And5.Exercise28"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SoftwareTestingExercisesOfChapter4And5"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Width="500">
            <TextBlock
                FontSize="14"
                FontWeight="Bold"
                Text="Dãy số X:" />
            <TextBlock
                Margin="0,5"
                Text="{Binding XString}"
                TextWrapping="Wrap" />
            <TextBlock
                FontSize="14"
                FontWeight="Bold"
                Margin="0,10,0,0"
                Text="Trung bình cộng của dãy X:" />
            <TextBlock
                FontSize="14"
                Foreground="Blue"
                Margin="0,5"
                Text="{Binding AverageOfXString}" />
        </StackPanel>
    </Grid>
</Window>
